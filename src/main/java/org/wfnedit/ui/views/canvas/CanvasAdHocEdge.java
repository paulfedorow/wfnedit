package org.wfnedit.ui.views.canvas;

/**
 * Provides the presentation for an ad-hoc arrow on the canvas. Used while the creation of new edges.
 */
public class CanvasAdHocEdge extends CanvasArrow {
    /**
     * Constructs an ad-hoc arrow view.
     *
     * @param viewModel the view model
     */
    public CanvasAdHocEdge(ViewModel viewModel) {
        super(viewModel);
    }
}
